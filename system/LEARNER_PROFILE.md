# LEARNER_PROFILE — Dave

This file is a living model of how Dave learns best. It starts as hypotheses (evidence-based defaults from `METHOD.md`) and is rewritten by `/learn-review` as real session data accumulates in `progress/`. Every claim below should eventually be traceable to a `progress/` log, not vibes.

**Last updated:** 2026-07-18 (initial creation — no session data yet)
**Sessions logged:** 0

## Known context (self-reported, not yet evidence-based)

- Says he is "not always a great learner" — treat this as motivation for the system's structure (retrieval + spacing + hands-on by default), not as a fixed trait. Re-evaluate once data exists.
- Comfortable with hands-on/technical work generally (runs a homelab, builds software) — hypothesis: labs and real-system practice will outperform pure reading for him. **Unconfirmed — test this.**
- Full Splunk University access — official courseware should be preferred as primary source material where available.

## Open questions to resolve via early sessions

- Best time of day for focused study?
- Session length before quiz accuracy drops?
- Does he retain more from a lab-first or read-first session order?
- Does audio (NotebookLM overviews, e.g. on a ride) actually stick, or is it good for exposure only, needing reinforcement via cards?
- Card format preference: cloze deletion vs. Q&A vs. scenario-based (SPL "what would you run for X")?

## Current defaults in effect (from METHOD.md, unmodified)

- SM-2 spaced repetition for all cards.
- Sessions interleave: due cards → short note → hands-on lab step → interleaved quiz → log.
- Hands-on labs run against live splunk-am06, not simulated data.

## Revision log

- 2026-07-18 — File created. No adaptations yet; baseline defaults active until first `/learn-review`.
